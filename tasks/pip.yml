- name: install python3.8 libraries
  pip:
    name: "{{ item }}"
    executable: /home/zanedufour/.pyenv/shims/pip3.8
    extra_args: "--user"
  loop:
    - invoke
    - pipx

- name: Install packages with pipx
  command: "pipx install --python python3.8 {{ item }}"
  loop:
    - black[d]
    - pre-commit
